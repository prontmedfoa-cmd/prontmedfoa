<%- include('../layouts/header') %>

<%- include('../layouts/sidebar') %>

<div class="container mt-4">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start gap-3 mb-4">
        <div>
            <h2><%= title %></h2>
            <p class="text-muted"><%= description %></p>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <p>Esta é a tela inicial do módulo <strong><%= title %></strong>. Em breve, aqui serão construídas as interfaces completas de registro, edição e consulta de informações dentro do prontuário.</p>
            <p class="text-muted">Enquanto isso, acesse os pacientes e o prontuário para continuar o fluxo de cuidado.</p>
        </div>
    </div>
</div>

<%- include('../layouts/footer') %>