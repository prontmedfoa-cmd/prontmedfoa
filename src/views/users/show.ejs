<%- include('../layouts/header') %>

<%- include('../layouts/sidebar') %>

<div class="container mt-4">

    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start gap-3 mb-4">
        <div>
            <h2>Detalhes do Usuário</h2>
            <p class="text-muted">Visualize as informações completas do usuário.</p>
        </div>
        <div class="d-flex gap-2">
            <a id="editLink" class="btn btn-secondary">Editar</a>
            <a href="/admin/users" class="btn btn-outline-secondary">Voltar</a>
        </div>
    </div>

    <div id="userSummary" class="card">
        <div class="card-body">
            <div class="text-center py-4 text-muted">Carregando usuário...</div>
        </div>
    </div>

</div>

<script src="/js/user-show.js"></script>

<%- include('../layouts/footer') %>
