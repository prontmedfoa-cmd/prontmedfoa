<%- include('../layouts/header') %>

<%- include('../layouts/sidebar') %>

<div class="container-fluid mt-3">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-start gap-3 mb-4">
        <div>
            <h2>Consultas</h2>
            <p class="text-muted">Selecione um paciente para rever consultorias e abrir novos atendimentos clínicos.</p>
        </div>
        <a href="/consultations/new" class="btn btn-primary">Nova Consulta</a>
    </div>

    <div class="card mb-4">
        <div class="card-body">
            <div class="row g-3 align-items-end">
                <div class="col-md-6">
                    <label class="form-label">Paciente</label>
                    <select id="patientSelect" class="form-control"></select>
                </div>
                <div class="col-md-3 d-grid">
                    <button id="btnLoadConsultations" class="btn btn-primary">Buscar consultas</button>
                </div>
            </div>
        </div>
    </div>

    <div class="card">
        <div class="card-body">
            <div class="table-responsive">
                <table class="table table-striped align-middle">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>Paciente</th>
                            <th>Data</th>
                            <th>Diagnóstico</th>
                            <th>Médico</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody id="consultationsTable"></tbody>
                </table>
            </div>
        </div>
    </div>
</div>

<script src="/js/consultations.js"></script>

<%- include('../layouts/footer') %>